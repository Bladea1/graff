<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.GraffZone" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Compose handles theme; keep as DayNight -->
        <item name="android:statusBarColor">@android:color/black</item>
    </style>
</resources>
